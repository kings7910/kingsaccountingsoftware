import {describe,expect,it} from "vitest";import {demoVehicles,milesUntilService,serviceState,validateVehicle} from "@/lib/fleet";describe("fleet workflows",()=>{it("tracks service intervals",()=>{expect(milesUntilService(demoVehicles[1])).toBe(420);expect(serviceState(demoVehicles[1])).toBe("Due soon")});it("rejects duplicate unit numbers",()=>expect(validateVehicle({...demoVehicles[0]},demoVehicles,"different").unit).toBeTruthy())});
